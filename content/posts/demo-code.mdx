---
title: "代码高亮和 Mermaid 图表测试"
date: "2026-01-24"
tags: ["测试", "演示"]
description: "测试博客的代码语法高亮和 Mermaid 图表支持"
---

本文用于测试博客的代码语法高亮和 Mermaid 图表功能。

## JavaScript/TypeScript 代码高亮

```javascript
// 示例：React 组件
import { useState, useEffect } from 'react'

function Counter({ initialCount = 0 }) {
  const [count, setCount] = useState(initialCount)

  useEffect(() => {
    document.title = `Count: ${count}`
  }, [count])

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  )
}
```

## Python 代码高亮

```python
# 示例：斐波那契数列
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

# 计算前 10 个斐波那契数
fib_sequence = [fibonacci(i) for i in range(10)]
print(fib_sequence)
```

## TypeScript 类型定义

```typescript
interface User {
  id: string
  name: string
  email: string
  createdAt: Date
}

type UserRole = 'admin' | 'editor' | 'viewer'

interface ExtendedUser extends User {
  role: UserRole
  permissions: string[]
}
```

## Mermaid 流程图

```mermaid
flowchart TD
    A[开始] --> B{是否需要高亮?}
    B -->|是| C[使用 Shiki]
    B -->|否| D[使用普通代码块]
    C --> E[显示语法颜色]
    D --> E
    E --> F[结束]
```

## Mermaid 时序图

```mermaid
sequenceDiagram
    participant User
    participant Blog
    participant MDXRemote
    participant Shiki

    User->>Blog: 访问博客文章
    Blog->>MDXRemote: 解析 MDX 内容
    MDXRemote->>Shiki: 请求代码高亮
    Shiki-->>MDXRemote: 返回带高亮的 HTML
    MDXRemote-->>Blog: 返回渲染后的内容
    Blog-->>User: 显示文章
```

## Mermaid 状态图

```mermaid
stateDiagram-v2
    [*] --> Idle
    Idle --> Processing: 开始处理
    Processing --> Success: 处理成功
    Processing --> Error: 处理失败
    Error --> Idle: 重试
    Success --> [*]
```

## Mermaid 类图

```mermaid
classDiagram
    class Component {
        +props
        +state
        +render()
        +componentDidMount()
        +componentWillUnmount()
    }
    class BlogPost {
        +title: string
        +date: Date
        +content: string
        +tags: string[]
    }
    class CodeBlock {
        +code: string
        +language: string
        +render()
    }

    BlogPost --> Component : extends
    CodeBlock --> Component : extends
```
